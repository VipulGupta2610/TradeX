events {}

http {

    upstream backend_servers {
        server backend1:2222;
        server backend2:2222;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://backend_servers;
        }
    }

}